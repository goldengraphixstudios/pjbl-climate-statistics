PJBL Climate Statistics - Project Work Log
==========================================

Purpose
-------
This file is the running log for:
- conversation decisions
- repository state observations
- code changes made in this workspace
- SQL / Supabase changes that were requested or applied
- commits created from this workspace
- next-step handoff notes

Logging Rules
-------------
- Append new entries at the bottom.
- Use local date/time when possible.
- For code changes, include affected files.
- For database changes, include the SQL file or dashboard action.
- For commits, include commit hash, message, and whether it was pushed.
- For unresolved issues, end the entry with "Open issues:".

Entry Format
------------
[YYYY-MM-DD HH:MM] Type: summary
- Details
- Files:
- Commit:
- Push:
- Open issues:

Initial Context
---------------
[2026-03-09] Workspace linked to remote:
- Remote: https://github.com/goldengraphixstudios/pjbl-climate-statistics.git
- Local path: C:\Users\AJHAY\Desktop\pjbl-climate-statistics-main
- Note: This folder did not contain .git when work began, so a new local git repository was initialized and linked to the remote.

[2026-03-09] Repo state summary reconstructed from code + Claude transcript:
- App is a React + TypeScript + Vite climate-statistics learning platform.
- Roles: student, teacher, admin.
- Students use custom credential verification via Supabase RPC plus local fallback.
- Teacher/admin login uses Supabase Auth.
- Data model includes users, classes, class_students, responses, feedback, student_progress, and student_state.
- The app is mid-migration from localStorage-heavy behavior to Supabase-backed behavior.
- Combined teacher/admin portal exists in code.
- Student portal gating is intentional: next section unlocks after prior submission, teacher feedback, and student acknowledgement.

[2026-03-09] Important current findings:
- GitHub remote is reachable.
- Local source tree still contains known feedback-path issues:
  - Admin portal feedback button passes username instead of UUID in some paths.
  - Feedback write path may still break if public.users IDs do not match auth.users IDs.
  - RLS policies in supabase/schema.sql currently assume users.id = auth.uid() in multiple places.
  - seed.sql example uses gen_random_uuid() placeholders, which would break that identity assumption if used as-is.
- profilesService.ts currently resolves the current profile by id only; no email fallback is present in this tree.

[2026-03-09] Build verification:
- `npm run build` did not complete in this environment because `tsc` is not available on PATH.
- Build status remains unverified from this workspace until dependencies/tooling are corrected.

How To Use Going Forward
-----------------------
- Every time we change code, add a new log entry here.
- Every time we run SQL in Supabase, add the script name or summary here.
- Every time we commit, add the hash and message here.
- If a change was discussed but not actually applied, note that explicitly.

[2026-03-09 23:12] Fix: feedback submission path hardened
- Diagnosed from browser console screenshot plus exported Supabase tables.
- Confirmed `public.users` stores app-level UUIDs, while staff login was still using the Supabase Auth session UUID in app state.
- Confirmed the admin portal feedback buttons were passing `username` instead of `student UUID` for pre/post assessment feedback actions.
- Confirmed feedback inserts were still writing `created_by` from `auth.getUser().id`, which can mismatch `public.users.id`.
- Patched the app to use `public.users.id` as the internal app identity for staff login, fall back to email when resolving the current profile, resolve `feedback.created_by` through the app users table, and pass the correct student UUID from the staff tables.
- Files:
  - src/pages/portals/AdminPortal.tsx
  - src/services/feedbackService.ts
  - src/services/profilesService.ts
  - src/components/teacher/FeedbackPanel.tsx
  - src/pages/auth/AdminLogin.tsx
  - src/App.tsx
- Commit: not yet committed
- Push: not yet pushed
- Open issues:
  - RLS in Supabase may still assume `public.users.id = auth.uid()` in multiple policies. If feedback still fails after this frontend patch, the next check is the live SQL policies.

[2026-03-09 23:29] Fix: student section locking and status display aligned with actual workflow
- Student dashboard gating was still requiring feedback acknowledgement before unlocking the next section.
- Performance summary was showing `pending` whenever no numeric score existed, even if the activity had already been submitted and feedback had already been sent.
- Pre-assessment and post-assessment pages could still be reopened after a final response existed, which allowed accidental resubmission.
- Updated dashboard gating so the next lesson unlocks after the previous section has both a submission and teacher feedback.
- Updated dashboard labels to show `Feedback ready` when feedback exists but has not yet been acknowledged.
- Updated performance summary to show lifecycle state (`submitted`, `feedback ready`, `completed`) when no numeric score exists yet.
- Updated pre/post assessment pages to poll for feedback and become read-only after a final response already exists.
- Files:
  - src/pages/portals/StudentPortal.tsx
  - src/pages/student_sections/PerformanceSummary.tsx
  - src/pages/student_sections/PreAssessment.tsx
  - src/pages/student_sections/PostAssessment.tsx
- Commit: not yet committed
- Push: not yet pushed
- Open issues:
  - Lesson 1/2/3 final-output pages may still allow re-entry after submission depending on how each lesson persists its final state. Pre/post are now guarded explicitly.

[2026-03-09 23:43] Diagnosis: feedback rows are not visible to student-side anon requests
- Queried live Supabase directly with the frontend anon key.
- Confirmed the unified `responses` row exists for Gabriel under activity_type `pre`.
- Confirmed there were no visible feedback rows from anon reads for Gabriel at the time of inspection.
- Reproduced a signed staff insert directly against Supabase and confirmed feedback insert works when authenticated as staff.
- Conclusion: the student app is blocked by current feedback table visibility/policy behavior, not by the dashboard code alone.
- Additional code updates:
  - all submitted dashboard sections now lock after final submission
  - pre/post unified response rows now also carry `part1Score` and `part1GroupScores` for summary display
- Files:
  - src/pages/portals/StudentPortal.tsx
  - src/pages/student_sections/PreAssessment.tsx
  - src/pages/student_sections/PostAssessment.tsx
- Commit: not yet committed
- Push: not yet pushed
- Open issues:
  - Supabase SQL must be updated so student-side reads can see teacher feedback in the current custom-auth design.

[2026-03-10 00:08] Fix: staff and student feedback views now read the same live Supabase rows
- Added ordered Supabase fetches for `responses` and `feedback` so latest updates win in the UI.
- Added `getResponsesForStudents()` to support staff-side pre/post tables from the database instead of browser-local `assessmentScores`.
- Updated Admin pre/post response tables to read answers and score from live `responses` rows and keep feedback text/acknowledgement from live `feedback` rows.
- Updated Student Performance Summary to label visible feedback explicitly as teacher feedback while continuing to derive the auto-scored assessment result from the unified `responses` row.
- Live verification at time of change:
  - anon read of `responses` returned Gabriel's `pre` submission
  - anon read of `feedback` returned zero rows
  - this means missing feedback in the UI may still be true backend state, not just stale frontend rendering
- Files:
  - src/services/responsesService.ts
  - src/services/feedbackService.ts
  - src/pages/portals/AdminPortal.tsx
  - src/pages/student_sections/PerformanceSummary.tsx
- Commit: not yet committed
- Push: not yet pushed
- Verification limits:
  - `npm run build` could not complete in this shell because `tsc` is not available on PATH

[2026-03-10 00:23] Fix: pre/post analytics UI for very small class sizes
- Pre/post analytics cards now derive tested counts, scores, and competency groups from live Supabase `responses` rows instead of old local browser score state.
- Frequency-of-responses charts now hide zero-width labels and render as rounded pill bars, which prevents the stray `0` labels seen at the far right when only one student has answered.
- Boxplot sections now render a cleaner single-point marker for `n=1` instead of a nearly invisible collapsed box, and show a short `n / median` caption.
- Files:
  - src/pages/portals/AdminPortal.tsx
  - src/styles/AdminPortal.css
- Commit: not yet committed
- Push: not yet pushed

[2026-03-10 00:41] Feature: active combined portal lesson scoring scaffold
- Added dedicated Teacher/Admin tabs for `Lesson 1 Outputs`, `Lesson 2 Outputs`, and `Lesson 3 Outputs` in the active combined `AdminPortal`.
- Each lesson tab now reads live Supabase `responses` rows, shows a simple submission preview, supports score entry, and saves teacher scores back through `responses.teacher_score`.
- Saving a score reuses the existing response refresh path so class record and student performance summary can pick up the updated lesson score from the same backend field.
- Added lesson-specific table styling for submission metadata, score inputs, and feedback status.
- Files:
  - src/pages/portals/AdminPortal.tsx
  - src/styles/AdminPortal.css
- Commit: not yet committed
- Push: not yet pushed
- Verification limits:
  - `npm run build` still not verified in this shell because `tsc` is unavailable on PATH

[2026-03-10 01:02] Fix: deployment-safe lesson assets and Lesson 1 key warnings
- Updated Lesson 1 visual-guide and scatterplot image URLs to use `import.meta.env.BASE_URL`, which fixes GitHub Pages 404s caused by root-relative `/...` asset paths.
- Updated Lesson 2 scenario image URLs to use the same deployment-safe base path.
- Replaced two unkeyed mapped fragments in Lesson 1 with keyed `React.Fragment` wrappers to remove the repeated React list-key warning in the browser console.
- Clarification:
  - the `public.lessons` table is not the source of lesson output submissions or lesson scoring
  - lesson submissions and teacher scores are currently stored in `public.responses`
- Files:
  - src/pages/student_sections/Lesson1.tsx
  - src/pages/student_sections/Lesson2.tsx
- Commit: not yet committed
- Push: not yet pushed

[2026-03-10 01:16] Fix: student-side runtime crash and noisy realtime warning
- Added a direct `React` import in `Lesson1.tsx` so the new keyed `React.Fragment` usage no longer crashes with `ReferenceError: React is not defined`.
- Removed the student dashboard realtime websocket subscription in `StudentPortal.tsx`; polling remains active every 10 seconds, so status refresh still works without the noisy websocket warning in the browser console.
- Added a dedicated bug tracking file for ongoing self-diagnosis and fixes:
  - `BUG_SELF_HEAL_LOG.txt`
- Local verification:
  - repo-local `tsc` and `vite` binaries executed successfully from the workspace shell
- Files:
  - src/pages/student_sections/Lesson1.tsx
  - src/pages/portals/StudentPortal.tsx
  - BUG_SELF_HEAL_LOG.txt
- Commit: not yet committed
- Push: not yet pushed

[2026-03-10 01:29] Feature: lesson result tabs now include exports and summary metrics
- Added lesson-tab CSV export and printable/PDF-style output for `Lesson 1 Outputs`, `Lesson 2 Outputs`, and `Lesson 3 Outputs`.
- Added small lesson analytics chips to the active lesson tabs:
  - submitted outputs
  - scored outputs
  - feedback sent
  - average score
- Restored acknowledgment-based unlocking in the student dashboard so the next section now waits for:
  - previous submission
  - feedback exists
  - feedback acknowledged
- Added a separate verification checklist file:
  - `PROJECT_VERIFICATION_CHECKLIST.txt`
- Files:
  - src/pages/portals/AdminPortal.tsx
  - src/styles/AdminPortal.css
  - src/pages/portals/StudentPortal.tsx
  - PROJECT_VERIFICATION_CHECKLIST.txt
  - PROJECT_PHASE_CHECKLIST.txt
- Local verification:
  - repo-local `tsc` and `vite` binaries executed successfully from the workspace shell
- Commit: not yet committed
- Push: not yet pushed

[2026-03-10 01:38] Fix: class record now uses actual assessment scores for pre/post
- Updated `getClassRecord()` so pre/post values come from `responses.answers.part1Score` first, with `teacher_score` only as fallback.
- Lesson 1/2/3 class record values continue to come from `responses.teacher_score`.
- This aligns class record behavior with the rest of the app:
  - assessments are auto-scored
  - lessons are teacher-scored
- Files:
  - src/services/submissionsService.ts
- Local verification:
  - repo-local `tsc` and `vite` binaries executed successfully from the workspace shell
- Commit: not yet committed
- Push: not yet pushed

[2026-03-10 01:44] Documentation: handover and verification files added
- Added handover documentation covering:
  - active portals
  - Supabase tables
  - scoring model
  - feedback flow
  - deployment notes
- Added a separate verification checklist for functional QA.
- Files:
  - HANDOVER_DOCUMENTATION.md
  - PROJECT_VERIFICATION_CHECKLIST.txt
- Commit: not yet committed
- Push: not yet pushed

[2026-03-10 01:53] Phase 1 consistency: unified submission metadata added to responses
- Standardized `responses.answers` payloads across pre/post and lesson finals by adding a shared `__meta` block with:
  - `schemaVersion`
  - `source`
  - `activityType`
  - `submittedAt`
  - `username`
  - `stage`
- Applied to:
  - Pre-assessment part 1 + final
  - Post-assessment part 1 + final
  - Lesson 1 final output
  - Lesson 2 final output
  - Lesson 3 final output
- Files:
  - src/pages/student_sections/PreAssessment.tsx
  - src/pages/student_sections/PostAssessment.tsx
  - src/pages/student_sections/Lesson1.tsx
  - src/pages/student_sections/Lesson2.tsx
  - src/pages/student_sections/Lesson3.tsx
- Local verification:
  - repo-local `tsc` and `vite` binaries executed successfully from the workspace shell
- Commit: not yet committed
- Push: not yet pushed

[2026-03-10 02:07] Bugfix: student feedback acknowledgment access and live-data exports
- Fixed a student dashboard progression bug where already-submitted sections were being card-locked, which could prevent students from reopening a finished activity to view and acknowledge teacher feedback.
- Student dashboard now:
  - still blocks future sections until the previous feedback is acknowledged
  - but allows reopening an already submitted section so acknowledgment can actually happen
- Fixed Admin portal CSV exports for:
  - pre-assessment
  - initial survey
  - post-assessment
  - end-of-lesson survey
  so they now read from live Supabase `responses` rows instead of old local browser score state.
- Files:
  - src/pages/portals/StudentPortal.tsx
  - src/pages/portals/AdminPortal.tsx
- Local verification:
  - repo-local `tsc` and `vite` binaries executed successfully from the workspace shell
- Commit: pending follow-up checkpoint
- Push: pending follow-up checkpoint

[2026-03-10 11:28] Deep bug sweep: Admin analytics/source-of-truth cleanup
- Audited active Student portal, student Performance Summary, active Admin portal, and response/feedback services for live-data mismatches and likely runtime bugs.
- Found that the active Admin portal still mixed live Supabase response rows with legacy localStorage-only assessment data in multiple places:
  - Initial Survey visible student-response table
  - End-of-Lesson Survey visible student-response table
  - printable Pre-Assessment report
  - printable Post-Assessment report
  - printable Initial Survey report
  - printable End-of-Lesson Survey report
- Fixed those paths so they consistently use the already loaded Supabase-backed `responseRows` state instead of `getAssessmentScores()` / local browser state.
- Added `buildSurveyResponses()` helper for response-backed survey summaries.
- Retained existing lesson result/scoring flow and class record behavior.
- Files:
  - src/pages/portals/AdminPortal.tsx
- Local verification:
  - repo-local `tsc` executed successfully
  - repo-local `vite build` executed successfully
  - targeted browser sanity attempt was partially blocked by local tooling setup:
    - detached `vite` preview/dev server launch was unreliable in this shell
    - Puppeteer worked only when pointed at local Edge, but static `dist/` serving under plain `http.server` produced expected base-path 404s because the app is built for `/pjbl-climate-statistics/`
  - result: code-level verification is solid; full authenticated browser-flow QA still needs a proper preview/deploy target
- Residual note:
  - legacy inactive teacher portal file `src/pages/portals/PerformanceSummary.tsx` still contains old localStorage-era logic, but it is not the active combined portal path currently being used
- Commit: not yet committed
- Push: not yet pushed

[2026-03-10 11:41] Checkpoint committed and pushed
- Saved the current bug-sweep fixes in git and pushed them to `origin/main`.
- Commit:
  - `05013b7` `Fix student reopening and admin analytics data sources`
- Included in that commit:
  - Student dashboard reopening fix for submitted sections awaiting feedback acknowledgment
  - Admin portal survey/assessment table and print/report data-source cleanup to use live Supabase response rows
  - bug log update
  - work log update
- Git status after push:
  - clean working tree

[2026-03-10 12:08] Smoke test follow-up: favicon 404 cleanup
- Ran a live student smoke pass against the deployed GitHub Pages site using `gabriel_labriaga`.
- Confirmed:
  - student login succeeds
  - pre-assessment shows as completed
  - Lesson 1 is unlocked
  - Lesson 1 opens successfully
- Found one remaining console/network issue on deploy:
  - missing favicon request to `vite.svg`
- Fixed by removing the stale Vite starter favicon reference from:
  - index.html
- Local verification:
  - repo-local `tsc` executed successfully
  - repo-local `vite build` executed successfully
- Commit: pending
- Push: pending

[2026-03-10 14:22] README credentials refresh
- Updated `README.md` to include the current confirmed test credentials.
- Added live student credentials from the class list:
  - `gabriel_labriaga / labriaga066`
  - `thandie_arevalo / arevalo276`

[2026-03-10 14:13] Password reissue workflow + README refresh
- Confirmed from Supabase CSV exports that `public.users.hashed_password` stores bcrypt hashes only.
- Conclusion:
  - plaintext student passwords cannot be reconstructed from Supabase
  - blank password cells in the class list must be handled as credential reissue, not data re-read
- Added password reset/reissue support:
  - `src/services/classService.ts`
  - `src/components/teacher/StudentList.tsx`
  - `SUPABASE_PASSWORD_RESET_SQL.sql`
- Updated documentation:
  - `README.md`
  - `SUPABASE_SETUP.md`
- Purpose:
  - allow teacher/admin users to reissue a student password from the class list
  - keep local credential caches in sync after reset
  - document that lost plaintext passwords are not recoverable from Supabase

[2026-03-10 13:28] Final handover scope/problem/recommendation documents
- Added a client-facing summary text file:
  - `CLIENT_SCOPE_AND_RECOMMENDATIONS.txt`
- Purpose:
  - clearly separate delivered contract-side work
  - identify the actual active data sources
  - document problems caused by the premade mixed frontend/database state
  - list improvement items that are enhancements or extra scope rather than core backend integration
- Added a separate sign-off checklist:
  - `FINAL_SIGNOFF_QA_CHECKLIST.txt`
- Purpose:
  - provide a simple role-by-role final acceptance test list before handover
  - reduce ambiguity on whether the project is ready for final sign-off

[2026-03-10 13:39] Fix password column regression in List of Classes
- Diagnosed the blank password column issue in the class list/student list flow.
- Root cause:
  - class/student loading was only reading plaintext credentials from `studentPasswordCache`
  - but student registration also stores credentials in the older `studentDatabase` local key
  - if the first cache is missing or stale, the UI showed blank passwords even though a recoverable credential cache still existed locally
- Fix:
  - updated `classService.getPasswordCache()` to merge both local credential stores
  - kept `studentPasswordCache` as the preferred source
  - added fallback recovery from legacy `studentDatabase`
- File:
  - `src/services/classService.ts`
- Local verification:
  - repo-local `tsc` executed successfully

[2026-03-10 13:02] Cleanup pass: lesson source-of-truth hardening
- Added a new shared `studentStateService` to centralize:
  - username/id resolution
  - `student_state` reads
  - `student_state` upserts
  - `student_state` deletes
- Fixed an important lesson-state persistence bug:
  - several lesson state paths were incorrectly trying to resolve students by `email` or `name` while being passed a username
  - this could silently fail and leave lesson draft state browser-only
- Updated Lesson 1 to hydrate from server-side `student_state` on load.
- Added consolidated server-side draft snapshots for:
  - Lesson 2
  - Lesson 3
  so browser-local state is no longer the only draft persistence path for those lessons.
- Added explicit source-of-truth documentation and Supabase cleanup SQL helper file:
  - `DATA_SOURCE_OF_TRUTH.md`
  - `SUPABASE_CLEANUP_SQL.sql`
- Updated handover and checklist docs to reflect the new source-of-truth model.
- Files:
  - src/services/studentStateService.ts
  - src/services/progressService.ts
  - src/pages/student_sections/Lesson1.tsx
  - src/pages/student_sections/Lesson2.tsx
  - src/pages/student_sections/Lesson3.tsx
  - DATA_SOURCE_OF_TRUTH.md
  - SUPABASE_CLEANUP_SQL.sql
  - HANDOVER_DOCUMENTATION.md
  - PROJECT_PHASE_CHECKLIST.txt
  - PROJECT_VERIFICATION_CHECKLIST.txt
- Local verification:
  - repo-local `tsc` executed successfully
  - repo-local `vite build` executed successfully
- Commit: pending
- Push: pending

[2026-03-10 14:35] Lesson 1 hydration regression fix
- Investigated a live mismatch where:
  - Student dashboard card showed Lesson 1 as completed
  - Opening Lesson 1 showed teacher feedback but progress reset to 0%
- Root cause:
  - Dashboard completion was driven by `responses` + `feedback`
  - Lesson 1 page was hydrating mostly from `student_state` / browser cache
  - when `student_state` read was unavailable or stale, the page fell back to an empty local state even though the submitted `responses.lesson1` row existed
- Fix applied:
  - Lesson 1 now hydrates from a merged state built from:
    - local `lesson1State`
    - Supabase `student_state`
    - submitted `responses.answers.lesson1State`
  - Added normalization so completed phase flags produce non-zero phase progress and completed phase markers
  - Mission Complete now persists a normalized final Lesson 1 state before upserting the final `responses` row
- Source-of-truth clarification:
  - `student_progress` is currently legacy / transitional and is not the active source of truth for dashboard state
  - active lesson state is currently driven by `responses`, `feedback`, `student_state`, and browser caches
- Files:
  - `src/pages/student_sections/Lesson1.tsx`
- Local verification:
  - repo-local `tsc -b` passed
- Commit: pending
- Push: pending
