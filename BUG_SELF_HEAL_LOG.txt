PJBL Climate Statistics - Bug / Self-Heal Log
=============================================

[2026-03-10 01:10] Student-side runtime and deployment audit

Observed issues
- Lesson 1 crashed in the browser with `ReferenceError: React is not defined`.
- Lesson 1 and Lesson 2 used root-relative image paths (`/...`) that break under GitHub Pages base path `/pjbl-climate-statistics/`.
- Student portal emitted a Supabase realtime websocket warning:
  - `WebSocket is closed before the connection is established`
- Lesson 1 showed React list-key warnings from mapped fragments.

Diagnosis
- `React.Fragment` was introduced in `Lesson1.tsx`, but the file only imported named hooks from `react`.
- Static images were being requested from site root instead of Vite base URL.
- Student dashboard already polls feedback/status every 10 seconds, so realtime subscription was redundant and noisy.
- Two mapped fragment blocks in Lesson 1 lacked stable fragment keys.

Self-heal actions applied
- Added a direct `React` import to `src/pages/student_sections/Lesson1.tsx`.
- Replaced Lesson 1 image URLs with `import.meta.env.BASE_URL`-based paths.
- Replaced Lesson 2 scenario image URLs with `import.meta.env.BASE_URL`-based paths.
- Replaced unkeyed mapped fragments in Lesson 1 with keyed `React.Fragment`.
- Removed student dashboard realtime websocket subscription from `src/pages/portals/StudentPortal.tsx`, keeping polling active.

Files touched
- src/pages/student_sections/Lesson1.tsx
- src/pages/student_sections/Lesson2.tsx
- src/pages/portals/StudentPortal.tsx

Notes
- `public.lessons` is not the source of lesson submissions or teacher lesson scores.
- Live lesson output + scoring currently use `public.responses`.
- If the live website still shows the old behavior after these fixes, the deployed build is stale and needs a new deploy from the updated repo.

[2026-03-10 11:28] Admin analytics stale-data audit

Observed issues
- The active Admin portal still rendered some survey tables and printable reports from legacy local browser assessment state rather than the live Supabase `responses` rows already loaded for the page.
- This created a likely mismatch where:
  - on-screen lesson/pre/post data could look correct in one area
  - survey tables or print/export views could still appear empty or stale

Diagnosis
- The active portal had already been partially migrated:
  - response rows were loaded from Supabase
  - lesson scoring/results used those rows
  - CSV exports had been moved to live data
- But several visible/printable branches still called the old localStorage-driven helpers.

Self-heal actions applied
- Replaced legacy localStorage-backed survey table rows with `responseRows`-backed data in:
  - Initial Survey tab
  - End-of-Lesson Survey tab
- Replaced printable report data sources for:
  - Pre-Assessment
  - Post-Assessment
  - Initial Survey
  - End-of-Lesson Survey
- Added `buildSurveyResponses()` helper so survey summaries also derive from current response rows.

Files touched
- src/pages/portals/AdminPortal.tsx

[2026-03-11 13:18] Student dashboard still blocked next lesson after previous completion

Observed issues
- Students could complete Lesson 1 but Lesson 2 remained locked.
- The same progression issue was expected to affect later lesson transitions as well.

Diagnosis
- The student dashboard still required lesson feedback and acknowledgment before unlocking the next section.
- That no longer matched the intended flow where the next section should open once the previous section is completed.

Self-heal actions applied
- Simplified section locking to require previous submission/completion only.
- Simplified dashboard status labels so submitted sections show as completed instead of waiting on feedback state.

Files touched
- src/pages/portals/StudentPortal.tsx

Verification
- repo-local TypeScript build succeeded
- repo-local Vite build succeeded

[2026-03-10 14:35] Lesson 1 page resets to 0% after live completion

Observed issue
- Student dashboard showed Lesson 1 as completed.
- Opening the Lesson 1 page showed teacher feedback but a `0%` progress bar and collapsed empty phase state.
- User confirmed lesson data still existed live in `student_state`.

Diagnosis
- Dashboard completion was being inferred from `responses` + `feedback`.
- Lesson 1 page hydration depended mainly on `student_state` and browser cache.
- If `student_state` read failed or returned stale data, the page could fall back to an empty local state even though the final submitted `responses.answers.lesson1State` payload existed.
- Mission Complete was also upserting the `responses.lesson1` row from the pre-normalized mission state, which could leave the stored final snapshot weaker than the UI-completed version.

Self-heal actions applied
- Added a merged hydration path in Lesson 1:
  - local cache
  - `student_state`
  - `responses.answers.lesson1State`
- Added Lesson 1 state normalization so completed phase flags restore sensible `completedPhases`, `unlockedPhase`, and per-phase progress values.
- Updated Mission Complete to persist the normalized final Lesson 1 state before saving the final response row.

Files touched
- src/pages/student_sections/Lesson1.tsx
- PROJECT_WORK_LOG.txt

Verification
- repo-local TypeScript build succeeded

[2026-03-10 15:05] Lesson 1 Activity 4 teacher feedback not reachable from active admin portal

Observed issue
- Student Lesson 1 Phase 1 Activity 4 includes a feedback box and revised-question flow.
- Active Teacher/Admin portal did not make that flow explicit:
  - action button was generic
  - submission detail did not show the research question content
- Student-side Activity 4 revision gate still depended on an older local teacher-feedback path instead of the live Supabase `feedback` row.

Diagnosis
- The active admin portal already had a working `lesson1` feedback action, but it was not obvious that it was the correct path for Activity 4.
- The student-side Activity 4 UI was not treating the live `feedback.lesson1` row as the authoritative feedback source for the question revision step.

Self-heal actions applied
- Lesson 1 Activity 4 feedback box now falls back to `serverFeedback.feedback_text`.
- Revised-question textarea and finalize button now unlock from the live feedback row.
- Admin Lesson 1 Outputs now includes the saved research question in the submission detail.
- Lesson 1 feedback action label changed to `Activity 4 Feedback`.

Files touched
- src/pages/student_sections/Lesson1.tsx
- src/pages/portals/AdminPortal.tsx

[2026-03-12 09:20] GitHub Pages admin login timed out from Supabase auth refresh loop

Observed issues
- Teacher/Admin login on GitHub Pages timed out even with correct credentials.
- Browser console showed repeated `auth/v1/token?grant_type=refresh_token` failures from the `github.io` origin, ending in CORS/failed fetch errors.

Diagnosis
- The live static site was bootstrapping a persisted Supabase Auth session and attempting to refresh a stale token before login.
- On the GitHub Pages host, that stale refresh path was enough to trap the UI in retries and eventually hit the app-side login timeout.

Self-heal actions applied
- Clear stale `sb-...auth-token` storage entries before Supabase client initialization on GitHub Pages.
- Disable persisted auth sessions and auto-refresh specifically for the `github.io` host.
- Clear stale auth storage again immediately before admin sign-in on GitHub Pages for a clean login attempt.

Files touched
- src/services/supabaseClient.ts
- src/pages/auth/AdminLogin.tsx

[2026-03-12 10:05] Lesson 3 admin reflection could depend on local timing instead of durable state

Observed issues
- Lesson 3 had several save paths where student work was definitely written locally, but durable server-side reflection relied on the page-level snapshot effect running afterward.
- Admin lesson output summaries also counted draft-only lesson state as if it were a final submission, which made Lesson 1 and Lesson 3 output counts/previews misleading.

Diagnosis
- `saveLesson3Phase2Activity2`, `saveLesson3Phase2Activity3`, `saveLesson3Phase3Activity1`, `saveLesson3Phase4PeerReview`, and `saveLesson3Phase4Reflection` did not all mirror their latest values into `student_state`.
- Admin preview logic treated any Lesson 1/3 row with state data as a final output, even when the row existed only as a draft proxy built from `student_state`.

Self-heal actions applied
- Mirrored the affected Lesson 3 save helpers into `student_state` using the same top-level keys that the student page and admin review consume.
- Updated admin lesson preview labeling so draft-only Lesson 1 and Lesson 3 rows show `Draft saved` with progress instead of pretending to be final outputs.
- Split the admin lesson summary cards into `Submitted outputs` and `Draft saves`.

Files touched
- src/services/progressService.ts
- src/pages/portals/AdminPortal.tsx

[2026-03-11 13:34] Student performance summary still used old feedback gating

Observed issues
- Student dashboard could unlock the next lesson after submission, but the active student Performance Summary still showed lessons as partially complete until feedback was acknowledged.

Diagnosis
- `src/pages/student_sections/PerformanceSummary.tsx` still computed lesson progress as 50% when a response existed without acknowledged feedback.
- This had become inconsistent with the newer unlock rule in `src/pages/portals/StudentPortal.tsx`.

Self-heal actions applied
- Simplified lesson progress computation so Lesson 1, Lesson 2, and Lesson 3 are treated as completed once their response row exists.
- Kept assessment completion submission-based as already intended.

Files touched
- src/pages/student_sections/PerformanceSummary.tsx

[2026-03-11 13:52] Lesson 1 Phase 4 values missing in admin review / stale class roster risk

Observed issues
- Lesson 1 Phase 4 final activity entries could appear missing in the admin review because the review renderer expected top-level Phase 4 fields, while the actual saved reflection values were nested under `phaseData[4].reflection`.
- Admin lesson tabs could also miss otherwise valid student work if the in-memory class roster was stale, because lesson rows are built from the current `classes` prop.

Diagnosis
- The active Lesson 1 Phase 4 save path stores reflection answers under `phaseData[4].reflection`, peer review under `phaseData[4].peerReview`, and upload metadata under `phaseData[4].upload`.
- The admin review modal only rendered a few top-level Phase 4 keys and therefore dropped the nested reflection answers.
- The app only loaded classes on mount and after local class mutations, so a stale roster could hide valid lesson state rows from admin tabs until a hard reload.

Self-heal actions applied
- Updated the Lesson 1 admin review renderer to show nested reflection fields, peer review details, rubric-style Phase 4 entries, and uploaded output metadata.
- Added periodic and focus-based class refresh in `src/App.tsx`, and mirrored the refreshed class list back into `teacherClasses` local storage.

Files touched
- src/pages/portals/AdminPortal.tsx
- src/App.tsx

[2026-03-11 14:02] Admin login status reset after class refresh

Observed issues
- Teacher/Admin login-status charts and `Pending/Logged In` badges could revert to all-pending after the app refreshed classes from Supabase, even if students had already logged in earlier in the same browser.

Diagnosis
- `hasLoggedIn` was only being toggled in in-memory React state during student login.
- `getAllClasses()` returns students with `hasLoggedIn: false`, so any later class reload overwrote the UI state.

Self-heal actions applied
- Added a persisted `studentLoginStatus` map in local storage.
- Class rows now overlay that map when converting Supabase classes into the portal’s legacy class shape.
- Student login writes into the persisted login-status map before updating UI state.

Files touched
- src/App.tsx

[2026-03-11 14:12] Login UI could appear stuck

Observed issues
- Report that logging in gets stuck.

Diagnosis
- The student login path still had a bogus `student-portal` page transition for admin-role logins through the student auth flow.
- Neither login screen had a timeout guard, so a slow or hung Supabase request could leave the UI showing `Logging in...` indefinitely.
- Username lookups were also not trimming whitespace before querying.

Self-heal actions applied
- Forced successful student-side login routing to `portal`.
- Added promise timeouts to both student and admin login flows.
- Normalized trimmed usernames before auth/profile lookups.

Files touched
- src/App.tsx
- src/pages/auth/StudentLogin.tsx
- src/pages/auth/AdminLogin.tsx

[2026-03-11 13:08] Admin lesson tabs excluded students with state-only work

Observed issues
- Some students had clearly answered Lesson 1 in `student_state`, but the admin Lesson 1 Outputs tab only showed one student.

Diagnosis
- The lesson-output tab filtered strictly on `responses` rows.
- Students with saved lesson work only in `student_state` were invisible in admin, even though their answers existed and the review modal merge logic could use them.

Self-heal actions applied
- Added synthetic draft lesson rows built from `student_state` for Lesson 1/2/3 when no `responses` row exists.
- Admin review remains available for those draft-only rows.
- Teacher score save is disabled for draft-only rows until a real `responses` row exists.

Files touched
- src/pages/portals/AdminPortal.tsx
- PROJECT_WORK_LOG.txt

[2026-03-10 15:22] Lesson feedback actions too ambiguous in admin portal

Observed issue
- Lesson output rows exposed only a single generic feedback action.
- Teacher/Admin could not easily distinguish between:
  - overall lesson feedback
  - Lesson 1 Activity 4 research-question feedback

Diagnosis
- The active backend only stores one live feedback row per `student_id + activity_type`.
- The UX therefore needed to be clarified without pretending the DB already had sub-activity feedback records for every lesson.

Self-heal actions applied
- Added a persistent overall `Feedback` button for all lesson rows.
- Added a second `Activity 4 Feedback` button for Lesson 1 rows.
- Added modal helper copy explaining the current backend limit.

Supabase impact
- No SQL/schema change required for this UI-only patch.
- Separate activity-level feedback records across lessons would require a future schema change.

Files touched
- src/pages/portals/AdminPortal.tsx
- src/components/teacher/FeedbackPanel.tsx
- PROJECT_WORK_LOG.txt
- Full browser-flow verification remains pending a reliable local preview/deploy target

[2026-03-10 12:08] Deployed smoke-test asset noise

Observed issue
- Live student smoke test on GitHub Pages still produced a 404 for `vite.svg`.

Diagnosis
- `index.html` still contained the default Vite starter favicon reference:
  - `<link rel="icon" type="image/svg+xml" href="./vite.svg" />`
- The project does not ship that file in `public/`, so every load produced a console/network error.

Self-heal action applied
- Removed the stale favicon reference from `index.html`.

Files touched
- index.html

[2026-03-10 13:02] Lesson draft persistence cleanup

Observed issues
- Lesson draft/completion state was still mixed across:
  - `responses`
  - `student_state`
  - browser `localStorage`
  - IndexedDB / `localforage`
- Some `student_state` persistence paths were incorrectly resolving the current student by `email` or `name` while being given a username.
- Result:
  - draft state could silently fail to save to Supabase
  - old local browser state could appear to be the source of truth

Diagnosis
- The resolver bug meant `student_state` upserts for lesson draft state were not reliable.
- Lesson 2 did not have a consolidated server-side lesson snapshot at all.
- Lesson 3 had partial `student_state` writes, but no consolidated page snapshot.

Self-heal actions applied
- Added a shared `studentStateService` with correct username/id resolution.
- Replaced bad username resolution logic in `progressService`.
- Lesson 1 now hydrates from server `student_state` on load.
- Lesson 2 now persists a consolidated draft snapshot into `student_state`.
- Lesson 3 now persists a consolidated draft snapshot into `student_state`.
- Added source-of-truth and SQL cleanup docs to reduce future confusion.

Files touched
- src/services/studentStateService.ts
- src/services/progressService.ts
- src/pages/student_sections/Lesson1.tsx
- src/pages/student_sections/Lesson2.tsx
- src/pages/student_sections/Lesson3.tsx
- DATA_SOURCE_OF_TRUTH.md
- SUPABASE_CLEANUP_SQL.sql

Verification
- repo-local TypeScript build succeeded
- repo-local Vite build succeeded

[2026-03-10 16:05] Lesson feedback ambiguity and missing review UI

Observed issues
- Lesson 1 needed two different teacher actions:
  - overall lesson feedback for the dashboard/performance summary
  - Activity 4 feedback for the research-question revision step
- The admin portal also lacked a practical way to inspect lesson submissions before grading.

Diagnosis
- The original `feedback` model allowed only one row per `student_id + activity_type`, so any UI split still wrote to the same backend row.
- Teachers were grading from a minimal table summary instead of a readable review surface.

Self-heal actions applied
- Added scoped feedback support in the app service layer.
- Added a separate Lesson 1 `Activity 4 Feedback` path backed by:
  - `feedback_scope = 'activity'`
  - `sub_activity_key = 'lesson1_phase1_activity4'`
- Kept overall lesson feedback as the source for performance summary / lesson banner.
- Added a read-only lesson `Review` modal in the admin portal for Lesson 1 / 2 / 3 submissions.
- Added `SUPABASE_FEEDBACK_SCOPE_SQL.sql` so the live database can support the scoped feedback model.

Files touched
- src/services/feedbackService.ts
- src/components/teacher/FeedbackPanel.tsx
- src/pages/student_sections/Lesson1.tsx
- src/pages/portals/AdminPortal.tsx
- supabase/schema.sql
- SUPABASE_FEEDBACK_SCOPE_SQL.sql

[2026-03-10 16:28] Assessment rows lacked a review path

Observed issue
- Pre-Assessment and Post-Assessment tables still required teachers to judge student work from a compressed row view only.

Diagnosis
- The new review modal had only been wired to lesson outputs, leaving assessment review inconsistent with the lesson grading flow.

Self-heal actions applied
- Added `Review` actions to pre/post assessment rows.
- Extended the review modal to render assessment answer sets, correctness markers, score, group scores, and survey responses.

Files touched
- src/pages/portals/AdminPortal.tsx

[2026-03-10 17:05] Lesson submits could stay local-only when profile lookup missed

Observed issues
- Lesson 2 submit actions were reported as not working.
- Lesson 1 admin review still showed stale blanks for revised question and some saved fields.
- The same risk pattern existed in Lesson 3 and assessment write/acknowledgement paths.

Diagnosis
- Multiple student-side Supabase calls used only `getMyProfile()?.id` to resolve the current student UUID.
- If that profile lookup returned no UUID, the save path stopped before `responses` was written, even though local/browser progress could still change.
- Admin review for Lesson 1 relied too heavily on the submitted `responses` snapshot and not enough on current `student_state`.

Self-heal actions applied
- Added `resolveStudentId(user.username)` fallback for active live save/read paths in Lesson 1, Lesson 2, Lesson 3, Pre-Assessment, and Post-Assessment.
- Lesson 1 revised-question finalize now also updates the live `lesson1` response row.
- Admin lesson review now merges `responses` with `student_state` so late edits do not appear blank in the review modal.

Files touched
- src/pages/student_sections/Lesson1.tsx
- src/pages/student_sections/Lesson2.tsx
- src/pages/student_sections/Lesson3.tsx
- src/pages/student_sections/PreAssessment.tsx
- src/pages/student_sections/PostAssessment.tsx
- src/pages/portals/AdminPortal.tsx

[2026-03-10 17:38] Lesson 2 buttons could fail silently on storage writes

Observed issues
- Lesson 2 observation/submit buttons were clickable but sometimes produced no visible state change.
- The same storage pattern existed across Lesson 2 final-output helpers and some Lesson 3 read/review paths.

Diagnosis
- Several Lesson 2 helper functions in `progressService.ts` still wrote directly to `localStorage` without using the project’s safe fallback helpers.
- If browser storage quota was exceeded or storage writes failed, the helper threw before the component could mark the activity submitted/completed.

Self-heal actions applied
- Replaced raw Lesson 2 helper storage writes with `safeGetAll` and `safeSetItemSync`.
- Switched related Lesson 3 and Lesson 2 interpretation/review getters to `safeGetAll` so fallback IndexedDB-backed data is still visible to the app/admin review.

Files touched
- src/services/progressService.ts

[2026-03-10 18:06] Lesson 2 drafts were being treated as final submissions

Observed issues
- Lesson 2 textareas/inputs could load in a locked state even when the student had only draft data or prior test entries.
- Phase 3 analysis sections could become locked after reloads because autosave timestamps were interpreted as submission state.

Diagnosis
- Lesson 2 load effects were inferring `submitted=true` from the existence of stored draft data instead of explicit submit markers.
- Some save helpers did not persist explicit submit flags, so the page could not reliably distinguish draft state from real submission state.

Self-heal actions applied
- Added explicit `submitted: true` persistence for the relevant Lesson 2 activity saves.
- Changed Lesson 2 hydration/load logic to lock only on explicit submitted markers.
- Removed timestamp-only locking for the Phase 3 analysis sections.

Files touched
- src/pages/student_sections/Lesson2.tsx
- src/services/progressService.ts

[2026-03-10 18:28] Lesson 2 intermediate UI needed updateable behavior

Observed issues
- User still reported Lesson 2 as unchanged: textareas remained locked and submit buttons still seemed to do nothing on previously touched activities.

Diagnosis
- Even with explicit submit flags fixed, the page was still hard-disabling many intermediate lesson inputs/buttons based on local section submitted flags.
- For this lesson, those intermediate activities are better treated as updateable checkpoints, not one-time immutable submissions.

Self-heal actions applied
- Removed hard-disable behavior from the main Lesson 2 intermediate activity inputs/buttons.
- Buttons now remain usable for resubmission/updates and relabel to `Update ...` after the first save.

Files touched
- src/pages/student_sections/Lesson2.tsx

[2026-03-10 19:16] Lesson 2 draft response missing from admin outputs

Observed issues
- Student-side Lesson 2 save/update buttons could appear to work, but Teacher/Admin `Lesson 2 Outputs` still showed `0 submitted outputs`.
- Some update-labeled Lesson 2 buttons still had leftover guard clauses that immediately returned and made the action effectively a no-op.

Diagnosis
- Admin results were driven by `responses.activity_type = 'lesson2'`, but Lesson 2 only wrote that row on the final Phase 4 submit.
- Intermediate saves only persisted to local storage and `student_state`, so staff never saw a row until the end.
- A couple of updateable actions still had stale `if (submitted) return` guards.

Self-heal actions applied
- Added draft `lesson2` response upsert from the student page so admin can see/save/review Lesson 2 work before final submission.
- Updated the admin Lesson 2 preview to distinguish draft vs final state.
- Added visible `Saved` / `Updated` button flash feedback on the main Lesson 2 save actions.
- Removed remaining no-op guards on updateable Lesson 2 actions.

Files touched
- src/pages/student_sections/Lesson2.tsx
- src/pages/portals/AdminPortal.tsx

[2026-03-10 19:34] Admin lesson review modal falling back to raw JSON

Observed issues
- Lesson 2 review was showing the entire `lesson2State` object as a JSON blob instead of a grading-friendly layout.
- Lesson 3 was still at risk of the same fallback because only Lesson 1 had a proper lesson-state merge + formatter.

Diagnosis
- `getLessonReviewSections()` had a specific structured renderer only for Lesson 1.
- Lesson 2 and Lesson 3 were falling through to the generic object dump.
- `mergeReviewResponse()` only merged remote `student_state` into Lesson 1 responses.

Self-heal actions applied
- Added structured review sections for Lesson 2 and Lesson 3.
- Added lesson-state merge support for Lesson 2 and Lesson 3 so admin review reflects the combined live state.

Files touched
- src/pages/portals/AdminPortal.tsx

[2026-03-10 20:02] Pre-assessment Part 1 save was falsely ending the whole assessment

Observed issues
- Student could answer Part 1, proceed to Part 2, then suddenly see the assessment end before finishing the survey.
- Because pre-assessment was not marked complete, Lesson 1 stayed correctly locked, but the student was blocked from continuing.

Diagnosis
- The pre-assessment page was using `!!existingResponse` as the lock condition for students.
- As soon as Part 1 upserted a `responses.pre` row with `stage = part1`, the polling reload treated that partial row like a fully submitted assessment and switched the UI into the submitted state.

Self-heal actions applied
- Changed the lock condition to require a final-stage response or a complete Part 2 payload.
- When only Part 1 exists, the page now continues/resumes in Part 2 instead of locking.
- Restored saved correctness state from the existing response when available.
- Tightened the visible Part 1 next-set disabled state through a dedicated guard.

Files touched
- src/pages/student_sections/PreAssessment.tsx

[2026-03-11 12:35] Admin mobile overflow and assessment-feedback scope cleanup

Observed issues
- On mobile, the combined Teacher/Admin portal expanded past the white container card and bled off-screen.
- Assessment tabs still exposed feedback controls even though the requested flow is to keep feedback only on lessons.
- Student progression needed to rely on pre-assessment completion alone for the `Pre -> Lesson 1` step.

Diagnosis
- The admin portal card allowed child content to overflow, while tab rows and data tables did not have strong mobile containment rules.
- Pre/post assessment result tables still carried lesson-style feedback columns and action buttons.
- Lesson gating was already mostly split by activity type, but the cleanup needed to preserve that rule while removing assessment feedback UI.

Self-heal actions applied
- Added mobile containment and horizontal scrolling to admin portal tabs and data-table containers.
- Removed pre/post feedback columns and feedback actions from admin assessment result tables.
- Kept lesson feedback paths unchanged and confirmed pre/post progression is submission-based rather than feedback-based.
- Removed remaining pre/post feedback display from the student performance summary and made assessment summary progress completion derive from submission alone.

Files touched
- src/pages/portals/StudentPortal.tsx
- src/pages/portals/AdminPortal.tsx
- src/pages/student_sections/PerformanceSummary.tsx
- src/styles/AdminPortal.css

[2026-03-11 12:58] Lesson 1 Activity 4 gating and stale admin lesson preview

Observed issues
- Lesson 1 Phase 1 Activity 4 still required teacher feedback before the revised-question field and `Finalize Question` button could be used.
- Admin Lesson 1 output rows showed stale/incomplete answer preview data even though newer values existed in `student_state`.

Diagnosis
- `canFinalize` and the revised-question textarea were still explicitly gated by `activity4Feedback`.
- The lesson result table preview used the raw `responses.lesson1` payload, while only the review modal merged in `student_state`.

Self-heal actions applied
- Removed the teacher-feedback requirement for Lesson 1 Activity 4 revision/finalization.
- Updated the Activity 4 copy and empty feedback-box message to reflect optional teacher feedback.
- Merged `student_state` into lesson result row previews before generating Lesson 1/2/3 admin submission summaries.

Files touched
- src/pages/student_sections/Lesson1.tsx
- src/pages/portals/AdminPortal.tsx

[2026-03-12 10:42] Admin refresh logout and empty-first-load

Observed issues
- On GitHub Pages, refreshing the site logged staff out immediately.
- On fresh admin login, class/data tabs often appeared empty until the user manually refreshed the page.

Diagnosis
- The Pages-specific auth workaround was clearing stored Supabase auth tokens on every boot and disabling session persistence entirely.
- `App.tsx` only loaded classes on mount and then on a delayed interval, so a login that happened after the initial anonymous load could leave the staff portal empty until a later reload.
- `AdminPortal.tsx` only refreshed feedback/responses when props changed, not on a short live polling cadence.

Self-heal actions applied
- Re-enabled Supabase session persistence on GitHub Pages while keeping auto-refresh disabled.
- Added app-session persistence/restoration in `App.tsx` and immediate class reloads after login/restore.
- Added live polling/focus refresh in `AdminPortal.tsx`.
- Added `currentUserId` fallback for teacher score writes.

Files touched
- src/services/supabaseClient.ts
- src/App.tsx
- src/pages/portals/AdminPortal.tsx
- src/services/responsesService.ts

[2026-03-12 09:09] Lesson 3 progress reset after refresh

Observed issues
- A student could finish nearly all of Lesson 3, see the progress bar at about 85%, then after an exit/refresh the lesson progress dropped back to 0%.
- The saved work itself was inconsistent to restore because the progress bar depended on browser-local progress state more than on the actual saved Lesson 3 activity records.

Diagnosis
- Lesson 3 progress restore depended on synchronous `localStorage` reads and a cached percent value.
- When saved Lesson 3 state fell back to IndexedDB/localforage, the page did not fully rehydrate those records on refresh.
- Several Lesson 3 save paths were also not mirroring the exact field names the page uses for restore, which made the fallback restore path fragile.

Self-heal actions applied
- Added async fallback-aware Lesson 3 persisted-state loading from browser storage.
- Changed Lesson 3 progress derivation so it recomputes from actual submitted activity flags instead of relying on a fragile cached number.
- Aligned the Lesson 3 persisted fields used by the student page restore flow.
- Verified in a browser repro that Lesson 3 came back at 85% after refresh when data existed only in fallback storage.

Files touched
- src/pages/student_sections/Lesson3.tsx
- src/services/progressService.ts

[2026-03-12 09:09] Lesson 2 Phase 2 Activity 4 white screen on submit

Observed issues
- In Lesson 2, Phase 2, Activity 4, uploading a PDF and clicking `Submit Output` could turn the page white.
- After reopening Lesson 2, the saved Activity 4 upload appeared missing and the student had to repeat the upload flow.

Diagnosis
- Activity 4 replaced the safe `blob:` preview URL with a persisted `data:` URL immediately after submit and rendered that `data:` URL inside an `iframe`.
- The browser could throw a `SecurityError` while handling the `data:` document, which matched the white-screen symptom.
- The Activity 4 UI also was not restoring the saved filename on return, so the saved upload looked missing even when it had been stored.
- The Lesson 2 upload helper was still incorrectly mirroring the Activity 4 upload into Lesson 1 state.

Self-heal actions applied
- Stopped feeding the saved `data:` PDF directly into the Activity 4 preview `iframe`.
- Kept inline preview for fresh `blob:` uploads, but after reload now show a stable saved-file state with the filename and an `Open saved PDF` link.
- Added saved filename restore through Lesson 2 local/server snapshot hydration.
- Removed the stray Lesson 1 mirror write from the Lesson 2 Activity 4 upload helper.
- Verified in a browser repro that submit no longer produced a page error/white screen and that, after reload, the saved PDF still appeared with its filename and open link.

Files touched
- src/pages/student_sections/Lesson2.tsx
- src/services/progressService.ts
